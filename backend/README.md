# Backend

No backend is required for the current tools. This directory reserves the boundary for a future Django service. If one is introduced, expose it under `/api/` and proxy that path through Nginx so existing tool URLs remain stable.

